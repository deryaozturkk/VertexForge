/*declare module '@models/Task' {
    export interface Task {
      // Task türüne ait alanlar buraya eklenebilir
      id: number;
      title: string;
      description: string;
      status: string;
      listId: number;
    }
  }
  */